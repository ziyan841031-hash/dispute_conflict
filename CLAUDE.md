# CLAUDE.md

This file provides context and conventions for AI assistants (like Claude) working in this repository.

## Repository Overview

**Repository:** `ziyan841031-hash/dispute_conflict`
**Status:** Early initialization — no application code exists yet.

The repository was initialized with a single README commit ("第一次提交" — "First commit" in Chinese). No source code, dependencies, tests, or build configuration are present at this time.

## Current Repository Structure

```
dispute_conflict/
├── .gitkeep        # Empty placeholder to preserve directory structure
├── README.md       # Project readme (currently minimal)
└── CLAUDE.md       # This file
```

## Git Workflow

### Branches
- Development work should happen on feature branches named `claude/<description>`.
- Push with tracking: `git push -u origin <branch-name>`.
- Never push directly to `main`/`master` without explicit permission.

### Commit Messages
- Use clear, descriptive English commit messages.
- Format: `<type>: <short summary>` (e.g., `feat: add dispute resolution module`).
- Common types: `feat`, `fix`, `refactor`, `docs`, `test`, `chore`.

## Development Guidelines (To Be Updated)

> Since the project has no code yet, the sections below are placeholders. Update this file as the project takes shape.

### Language & Framework
- **Not yet determined.** Update this section once a language/framework is chosen.

### Running the Project
- **Not yet configured.** Add setup, build, and run instructions here.

### Running Tests
- **Not yet configured.** Add test commands here once a testing framework is set up.

### Code Style & Conventions
- **Not yet established.** Document naming conventions, formatting rules, and linting tools here.

### Environment Variables
- **Not yet configured.** List required environment variables and their purpose here.

## Notes for AI Assistants

- **This is a minimal repo.** Do not assume any framework or language until code is added.
- **Check this file first** before making assumptions about project conventions.
- **Update CLAUDE.md** whenever significant structural or workflow changes are made.
- When the project grows, replace the placeholder sections above with accurate, specific information.
